
  {
    id: @product.id,
    name: @product.name,
    price: @product.price,
    image_path: @product.image_path,
    description: @product.description,
    formatted: {
      is_discounted: @product.is_discounted?
      tax: @product.tax
      total: @product.total
  }

  #Formatted section is meant to keep modified data separate from database data.